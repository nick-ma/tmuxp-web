{
  "name": "tmuxp-web",
  "version": "0.1.0",
  "main": "./app",
  "devDependencies": {
    "grunt": "",
    "grunt-hub": ""
  },
  "scripts": {
    "postinstall" :"cd ./app && npm install && bower install; cd ../; cd ./server && npm install; cd ../"
  }
}
